# Install in Codex

Unzip this package into the Codex skills directory so the folder containing `SKILL.md` is named:

`scientific-image2-visio`

Invoke explicitly:

`$scientific-image2-visio`

Final VSDX rendering requires a Windows machine with desktop Microsoft Visio and `pywin32`. Spec authoring and validation can run elsewhere.
